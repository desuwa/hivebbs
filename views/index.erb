<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <%= erb :assets %>
  <title><%= cfg(:app_name) %></title>
</head>
<body>
<div id="content">
<h1 id="app-title"><%= cfg(:app_name) %></h1>
<ul id="boards" class="list section">
  <% @boards.each do |board| %>
  <li><span class="rc">»</span> <a href="/<%= board[:slug] %>/"><%= board[:title] %></a></li>
  <% end %>
</ul>
</div>
<%= erb :footer %>
</body>
</html>
